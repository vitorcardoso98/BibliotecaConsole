package negocio;

import modelo.Usuario;

public class EmprestimoNegocio {

    //Recebe um usuário e verifica se o mesmo possui empréstimo ativo
    public boolean isStatus(Usuario usuario) {

        //passa o usuário para o método de consulta no banco
        //se a verificação de empréstimo retornar ativa o método retorna false
        //caso contrário retorna true
        return false;
    }
}
